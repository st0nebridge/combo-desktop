# PROFILE MANAGEMENT RULES
- Profile names must follow the pattern <provider>:<profile-name>
- Profile data must be isolated using Electron session partitioning
- Default profiles must be preserved during bulk operations
- Profile options must be properly merged with defaults
